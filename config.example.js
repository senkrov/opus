// For local development, copy this file to config.js and fill in your API server's URL.
// config.js is ignored by git.

// For production, you can create a config.js on the server.
// If the API is served from the same domain (e.g., via Nginx proxy),
// you can leave API_BASE_URL as an empty string.

window.APP_CONFIG = {
  // Example for local dev: 'http://123.45.67.89'
  // Example for production: ''
  API_BASE_URL: ''
};
